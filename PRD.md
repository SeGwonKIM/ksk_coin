# PRD: KSK 코인 — 채굴로 시작하는 가상 생활경제 세계

- 작성일: 2026-09-08
- 개정: 2026-09-08 (현실 경제 데이터 연동 취소 → 실제 건물·살림살이 기반 생활경제로 전환)
- 백서: [WHITEPAPER.md](WHITEPAPER.md) — 설계 근거와 수식, 검증값
- 배포 형태: 단일 `index.html` (HTML/CSS/JS, 외부 라이브러리 없음) — 브라우저에서 즉시 실행, GitHub Pages로 온라인 배포

## 1. 개요

**진짜 SHA-256 작업증명**으로 가상화폐 `KSK`를 캐고, 그 돈으로 **타워팰리스 같은 실제 건물을 사고**,
그 집 안에 **인테리어·가전·가구·살림살이를 채워 넣으며** 살아가는 가상 생활경제 세계다.

경제의 고리는 이렇게 닫힌다.

```
채굴 → KSK → (거래소) → 현금 → 건물 매입 → 그 건물이 곧 채굴장
                                   ↓              ↓
                              임대수익      수용전력·냉각효율
                                   ↓              ↓
                          살림살이 구매 ← 생활비·관리비·전기요금
                                   ↓
                        인테리어 등급 ↑ → 임대료·건물가치 ↑
```

건물은 **자산이면서 동시에 채굴 인프라**다. 원룸에서는 채굴기 두 대가 한계지만 타워팰리스는
수용 전력이 크고 냉각이 잘 되어 전기요금이 덜 든다. 남는 공간은 임대해 월세를 받는다.
그래서 "어디서 캐는가"가 "얼마를 버는가"를 결정한다.

### 1.1 이것은 시뮬레이션이다

- 실제 블록체인에 토큰을 발행하지 않고, 실제 화폐가 드나들지 않는다. 지갑·서명·P2P 네트워크도 없다.
- `KSK`와 게임 내 원화는 **현실 가치가 없는 숫자**다.
- 등장하는 건물 이름은 보드게임이 실제 지명을 쓰는 것과 같은 **랜드마크 표기**이며,
  가격·임대료·관리비는 **게임 균형을 위한 근사치**다. 실제 시장 정보가 아니고 투자 조언도 아니다.
- 현금 입출금, 실명 자산 거래, 실제 부동산·코인 시세 연동은 **v1 이후에도 만들지 않는다.**

### 1.2 이전 설계에서 바뀐 점

| | 이전 (취소) | 현재 |
|---|---|---|
| 현실 반영 방식 | 실제 환율·BTC 시세 API 연동 | **실제 건물·살림살이라는 현실 환경** |
| 전기요금 근거 | 환율(에너지 수입가) | 고정 단가 × 건물 냉각 효율 |
| 수요(심리) | 실제 BTC 24h 변동률 | 내부 수요 엔진 (채택률 + 평균회귀 랜덤워크) |
| 외부 네트워크 | `open.er-api.com`, CoinGecko | **없음 — 완전 오프라인 동작** |

## 2. 목표

1. **채굴을 가짜 진행바로 만들지 않는다.** 실제로 SHA-256 해시를 돌려 목표값 이하의 논스를 찾는다.
2. **PC와 휴대폰에서 모두 캘 수 있다.** 시작할 때 기기의 해시 속도를 재고 그에 맞춰 난이도를 정하므로
   느린 기기에서도 블록 간격이 비슷하다.
3. **발행 규칙을 실제 화폐처럼 못박는다.** 총 50억 개, 총 발행기간 50년, 반감기 25회.
4. **생활이 경제가 되게 한다.** 건물·가전·가구가 장식이 아니라 전기요금·임대료·자산가치로 계산에 들어간다.
5. **기능을 잘게 쪼개어 하나씩 완성한다.** 각 기능은 그것만으로도 화면에서 동작해야 한다.

## 3. 대상 사용자 / 플랫폼

- 대상: 채굴·화폐 발행·자산 경제가 어떤 원리로 돌아가는지 손으로 만져 보고 싶은 비전공자.
- 플랫폼: 데스크톱/모바일 브라우저(Chrome·Edge·Firefox·Safari 최신).
  `index.html` 더블클릭만으로 동작하고 서버·설치·회원가입·네트워크가 없다.
- 모바일: 화면이 꺼지면 채굴을 멈추고 돌아오면 자동 재개한다(`requestAnimationFrame`이 정지하므로).
  프레임 예산을 PC 12ms / 모바일 8ms로 나눠 발열과 배터리를 배려한다.

## 4. 발행 설계 (확정)

등비수열 합 `R0 × 반감기간격 × 2 = 총발행량` 을 이용해 요청 수치를 정확히 맞췄다.

| 항목 | 값 | 근거 |
|---|---|---|
| 총 발행 상한 | **5,000,000,000 KSK (50억)** | 요청 |
| 초기 블록 보상 | **2,500,000 KSK (250만)** | 250만 × 1,000 × 2 = 50억 |
| 반감기 간격 | **1,000 블록 = 2 게임년** | |
| 반감기 횟수 | **25회** | 25 × 2년 = 50년 |
| 총 블록 수 | **25,000** | 1,000 × 25 |
| 목표 블록 시간 | **17.52초** | 438,000초 ÷ 25,000 |
| 총 발행 기간 | **50 게임년** | 25,000 × 17.52초 = 438,000 게임시간 |
| 실제 소요 시간 | 약 **5.07일** 연속 채굴 | 시간 축척 1초 = 1게임시간 |
| 마지막 에라 보상 | 0.1490 KSK | 250만 ÷ 2²⁴ |
| 시작 시세 | **0.01원/KSK** (1원 = 100 KSK) | 50억 발행량에 맞는 단가 |

검산: 25에라 누적 발행 `4,999,999,850.99 KSK` + 마지막 블록 잔여 `149.01` = **정확히 50억**.
50년째에 발행이 끝나며 그 뒤로는 새 코인이 생기지 않는다(채굴 보상 0).

### 4.1 시간 축척

**1 실제초 = 1 게임시간.** 전기요금·관리비·임대료가 모두 이 축척을 쓴다.
1 게임년 = 8,760 게임시간 = 2.43 실제시간. 50년을 다 캐려면 약 5일이 걸린다.

## 5. 합의 방식 — 보안은 최대, 기기 자원은 최소

"보안성이 가장 좋으면서 기기 자원을 최소로 쓰는 방식"으로 요청받았다. 둘을 동시에 최대화하는
방식은 없다 — 맞바꾸는 관계다.

| 방식 | 보안의 근거 | 기기 자원 | 실전 |
|---|---|---|---|
| PoW 작업증명 | 해시파워 과반 확보 비용 | **최대** (CPU 100%) | 비트코인 16년, 가장 검증됨 |
| **PoS 지분증명** | **스테이크 몰수(슬래싱)** | **거의 0** (서명만) | 이더리움 2022~, 에너지 99.95%↓ |
| DPoS | 위임 대표 소수 | 낮음 | 중앙화로 보안 약화 |
| 공간증명 (Chia) | 디스크 용량 | 디스크 수명 소모 | 중간 |
| PoA | 신뢰된 검증자 | 거의 0 | 탈중앙 아님 |

→ **PoS + 슬래싱 + BFT 최종성**을 택했다. 다만 PoS 는 걸 코인이 있어야 검증자가 되므로
부트스트랩 문제가 있다. 이더리움이 PoW 7년 → 더 머지(2022)로 푼 길을 그대로 따른다.

### 5.1 참고 백서

| 우리 설계 | 대응 백서 |
|---|---|
| PoW 초기 분배 → PoS 전환 구조, 슬래싱, 미가동 누출, 최소 예치금 | **Casper FFG** — Buterin·Griffith 2017, `arXiv:1710.09437`. "기존 PoW 체인 위에 PoS 를 덧씌운다"는 정의가 우리 2단 구조와 같다 |
| 12초 서명 주기, 어테스테이션, 좌석제 | **Gasper** — Buterin 외 2020, `arXiv:2003.03052`. 이더리움 실제 합의. 슬롯이 정확히 12초 |
| 목표값 방식 PoW, 난이도 조정, 반감기·발행 상한 | **비트코인 백서** — Nakamoto 2008 |

### 5.2 파라미터

| 항목 | 값 |
|---|---|
| 1단계 해시 예산 | `DUTY` 3프레임당 1회 × 회당 `HASH_MS` (PC 3ms / 모바일 2ms) → **CPU 약 6%** |
| 1단계 풀 지분 | 폰 1대 = 4% (연속 지급, 직접 발견은 보상의 2% 보너스) |
| 검증자 1좌 | **32,000 KSK** (이더리움 32 ETH 를 흉내) |
| 서명 주기 | **12초에 해시 1회** — 이 사이 기기는 아무 일도 하지 않는다 |
| 네트워크 총 스테이크 | **세계 전체 발행량의 85%** (`STAKE_RATIO`) |
| 내 지분 수렴점 | **15%** — `share(m) = m/(m+r)` 의 고정점 `m* = 1−r` |
| 미가동 누출 | 검증을 끄면 초당 스테이크의 0.002% 감소 |
| 전송·거래 수수료 | **0.01%** (매수·매도 양방향) |

### 5.3 지분 폭주를 막는 조건 (구현 중 실측으로 발견)

네트워크 총 스테이크를 **내가 캔 양**에 비례시키면 폭주 고리가 생긴다.
`지분↑ → 보상↑ → 스테이크↑ → 지분↑`. 실측에서 **4% 로 출발한 지분이 80% 까지 치솟았다.**

원인은 `S.mined` 하나가 **세계 전체 발행량**과 **내 수령량**을 겸한 것이었다. 두 값을 분리하고
(`S.issued` / `S.mined`), 총 스테이크를 세계 발행량의 비율 `r` 로 잡으면 수렴한다.

```
지분 share(m) = m / (m + r)          (m = 내 보유 비중)
고정점: share(m) = m ⟹ m + r = 1 ⟹ m* = 1 − r
r = 0.85 → 내 지분은 15% 로 수렴
```

**불변식**: 내 수령량 ≤ 세계 발행량. 세계 발행량도 블록 단위가 아니라 연속으로 흘려야 한다 —
풀이 연속 지급하는 동안 블록 단위로만 올리면 이 불변식이 순간적으로 깨진다.

### 5.4 1단계 — 초기 분배 (가벼운 작업증명)

- 순수 JS 동기식 SHA-256. `crypto.subtle.digest`는 비동기라 논스마다 `await`하면 수십 배 느려지고
  `file://`에서 보안 컨텍스트 문제가 생길 수 있어 쓰지 않는다.
  → node의 `crypto`와 대조 검증 완료(빈 문자열·`abc`·한글 UTF-8·다중 블록 전부 일치).
- 블록 헤더 = `{높이}|{이전해시}|{보상}|{논스}`.
- 성공 판정은 비트코인과 같은 **목표값(target) 방식**: 해시 앞 8자리(32비트)를 정수로 보고
  `target = 2³² / 난이도` 이하면 성공. 선행 0 개수 방식은 한 단계가 16배로 뛰어 미세 조정이 불가능하다.
- 메인 스레드를 막지 않기 위해 한 프레임에 정해진 개수만 시도하고 `requestAnimationFrame`으로 양보한다.
  (Web Worker는 단일 파일 + `file://` 조합에서 Blob URL이 막힐 수 있어 쓰지 않는다.)
- **기기 캘리브레이션**: 시작 시 약 2.5초간 해시 속도를 5회 재고 뒤쪽 3개의 중앙값으로 확정한다.
  네트워크 규모를 `내 속도 ÷ 0.4`로 잡아 **지분 40%**에서 출발한다(블록 약 44초).
  느린 휴대폰은 난이도도 함께 낮게 잡히므로 체감 간격이 비슷하다.
- 장비 배수는 **난이도 목표를 그만큼 완화**하는 방식으로 구현한다. `N배 빠른 기계가 난이도 D를 푸는
  기대시간`과 `1배 기계가 D/N을 푸는 기대시간`은 근사가 아니라 **정확히 같다**(둘 다 `D/(속도×N)`).
  그래서 해시는 언제나 진짜로 돌면서 장비 효과가 정확히 반영된다.
- 경제 계산(지분·기대수익)은 실측값이 아니라 **기준 해시레이트**를 쓴다. 실측을 그대로 쓰면
  측정 잡음만으로 수익 추정이 두 배씩 흔들린다. 발열 같은 실제 변화만 아주 느리게 반영한다.

## 6. 기능 분해 — 잘게 쪼갠 목록

`✅ 완료(검증)` `🔨 진행` `⬜ 예정`. 각 기능은 그것만으로 화면에서 동작해야 한다.

### F0. 기반 — 화폐의 뼈대
| | 기능 | 상태 |
|---|---|---|
| F0.1 | 순수 JS SHA-256 (표준 벡터 검증) | ✅ |
| F0.2 | 작업증명 채굴 루프, 프레임 단위 양보 | ✅ |
| F0.3 | 기기별 해시레이트 캘리브레이션 (PC·모바일 공통) | ✅ |
| F0.4 | 발행 설계 50억 / 50년 / 반감기 25회 + 잔여 정산 | ✅ |
| F0.5 | 네트워크 해시레이트와 난이도, 채굴자 유입·이탈 | ✅ |
| F0.6 | `localStorage` 저장·불러오기·초기화 | ✅ |
| F0.7 | 모바일 화면 꺼짐 대응 (자동 정지·재개), dt 상한 | ✅ |
| F0.8 | 원장 로그 | ✅ |

### F1. 화폐 — 거래소·합의
| | 기능 | 상태 |
|---|---|---|
| F1.7 | 1단계 가벼운 PoW (듀티 사이클, CPU 약 6%) | ✅ |
| F1.8 | 2단계 PoS 전환(더 머지) · 스테이킹 · 좌석제 | ✅ |
| F1.9 | 12초 서명(어테스테이션) · 미가동 누출 | ✅ |

### F1. 거래소
| | 기능 | 상태 |
|---|---|---|
| F1.1 | 시세 엔진 — `기준가 × 희소성 × 수요`, 원가하한 | ✅ |
| F1.2 | 현실 경제 API 연동 제거 → 내부 수요 엔진으로 교체 | ✅ |
| F1.3 | 매수·매도 + 슬리피지 | ✅ |
| F1.4 | 시세 스파크라인 차트 | ✅ |
| F1.5 | 주문 수량 직접 입력 | ⬜ |
| F1.6 | 전송·거래 수수료 0.01% | ✅ |

### F2. 부동산 — 매물과 매매
| | 기능 | 상태 |
|---|---|---|
| F2.1 | 건물 데이터 테이블 9종 (원룸 40만원 ~ 가산 데이터센터 1.2억) | ✅ |
| F2.2 | 매물 목록 + 지역·용도·구매가능 필터 | ✅ |
| F2.3 | 매입 (현금 결제, 잔액 부족 시 버튼 비활성) | ✅ |
| F2.4 | 보유 목록 + 평가손익 | ✅ |
| F2.5 | 시세 변동 (장기 우상향 추세 + 평균회귀 랜덤워크) | ✅ |
| F2.6 | 매도와 차익 실현 (왕복 -6% 로 차익 루프 차단) | ✅ |
| F2.7 | 취득세 4.6% · 중개수수료 0.5% (보유세는 F5) | ✅ |
| F2.8 | KSK 로 직접 결제 | ⬜ |
| F2.9 | 대출 (LTV·이자) | ⬜ |

### F3. 채굴 인프라 — 건물이 채굴장이다
| | 기능 | 상태 |
|---|---|---|
| F3.1 | 채굴 장비를 휴대폰 5종으로 (배수·와트·가격), 대당 통신비 | ✅ |
| F3.2 | 회선 수 한도 (`kw × 6`) — 넘으면 배치 버튼 비활성 | ✅ |
| F3.3 | 운영 효율 계수 → **전기 + 통신비** 배수 (근거는 §7) | ✅ |
| F3.4 | 채굴장 지정 + 폰별 배치 (+/−), 건물 매도 시 자동 해제 | ✅ |
| F3.5 | 장비별 손익분기 시세 표시 | ✅ |
| F3.6 | 소음·민원 — 주거용 한도 80% 초과 시 벌금 + 300초 할인 정지 | ✅ |

### F4. 살림살이 — 집 안을 채운다
| | 기능 | 상태 |
|---|---|---|
| F4.1 | 품목 카탈로그 14종 (가전 5 · 가구 4 · 인테리어 5) | ✅ |
| F4.2 | 구매 화면 + 분류 필터, 보유 개수·수명 표시 | ✅ |
| F4.3 | 상시 평균 소비전력 → 전기요금 (채굴을 끄든 켜든 나간다) | ✅ |
| F4.4 | 인테리어 등급 → 임대료 배수 (최대 +50%, **주거용만**) | ✅ |
| F4.5 | 감가상각 — 수명이 끝나면 고장(전기 0·등급 0), 원가 60% 로 교체 | ✅ |
| F4.6 | 방·공간 배치 (평면 그리드) | ⬜ |

### F5. 생활 — 현금흐름
| | 기능 | 상태 |
|---|---|---|
| F5.1 | 전기요금 (채굴 + 살림 합산) | ✅ |
| F5.2 | 관리비·세금 월 정산 | ⬜ |
| F5.3 | 임대수익 | ✅ (F2 가 초당 적립 — F5 가 월말 정산으로 넘겨받는다) |
| F5.4 | 파산 판정과 회생 | ✅ (파산만) |

### F6. 세계 시간
| | 기능 | 상태 |
|---|---|---|
| F6.1 | 게임 시계 (1초 = 1시간), 경과 연수 표시 | ✅ |
| F6.2 | 연·월·일 표시와 월말 정산 이벤트 | ⬜ |
| F6.3 | 50년 타임라인 진행도 | ✅ (발행률) |

### F8. 직업·커리어 — 노동 소득
채굴과 자산 말고 **세 번째 소득원**. 시간을 팔아 안정적인 월급을 받고, 자격을 따서 더 좋은 자리로 간다.

| | 기능 | 상태 |
|---|---|---|
| F8.1 | 직업 테이블 (무직·알바·중소기업·대기업·공무원·부동산 중개사) — 월급·조건·시간 점유 | ⬜ |
| F8.2 | 취직 (지원 → 조건 검사 → 합격 확률) | ⬜ |
| F8.3 | 월급 수령 (F5.2 월 정산과 연동) | ⬜ |
| F8.4 | 근무 시간 점유 → 동시에 할 수 있는 활동 제한 | ⬜ |
| F8.5 | 자격·학력 (부동산 중개사 시험, 학위) → 직업 해금 | ⬜ |
| F8.6 | 승진·경력 누적 | ⬜ |
| F8.7 | **중개사 개업** — 남의 거래를 중개해 수수료를 받는다 (F2.7의 반대편) | ⬜ |

직업은 다른 층과 이렇게 맞물린다.
- 월급은 **채굴 초기의 전기요금 방어선**이다 — 코인을 급하게 팔지 않아도 된다.
- 대기업 취직은 **대출 한도(F2.9)**를 올린다 — 소득 증빙이 있으니까.
- 부동산 중개사는 **내 거래의 수수료(F2.7)를 면제**하고, 개업하면 수수료가 수입이 된다.

### F7. UX
| | 기능 | 상태 |
|---|---|---|
| F7.1 | 모바일 반응형 + 터치 타깃 | ✅ |
| F7.2 | 탭 네비게이션 — `data-tab` 만 달면 탭이 자동 생성 | ✅ |
| F7.3 | 순자산 요약 대시보드 (부동산·살림은 붙으면 자동 합산) | ✅ |

### 검증된 값

| 항목 | 실측 |
|---|---|
| SHA-256 정확성 | 표준 라이브러리와 전 항목 일치 |
| 1단계 CPU 점유 | 약 6% |
| 1단계 풀 지분 | 4.03% (설계 4%) |
| 더 머지 후 해시 | 0회 (PoW 루프 정지 확인) |
| 스테이크 지분 | 전량 예치 시 2.65% → 15% 수렴 |
| 전송 수수료 | 0.0100% (설계 0.01%) |
| 발행 검산 | 4,999,999,850.99 + 149.01 = 50억 |

### 진행 순서

~~`F1.2`~~ → ~~`F7.2`~~ → ~~`F2`~~ → ~~`F3`~~ → ~~`F4` (살림살이)~~ →
`F3.2~F3.4` (건물이 채굴장) → `F4.1~F4.3` (살림살이) → `F8.1~F8.3` (직업·월급) →
`F5.2~F5.3` (월 정산·임대) → `F2.5~F2.7` (부동산 시세·세금) → 나머지.

### 병렬 작업 — 스킬 8개

`index.html` 하나를 여러 에이전트가 동시에 고치면 충돌하므로, 파일 안을 **기능별 예약 구역**으로
나누고(`==== [F2:부동산] JS 시작 ====` 형태, 30개 마커) **확장 훅**(`tickHooks`/`renderHooks`)을
뒀다. 메인 루프와 `render()` 를 건드리지 않아도 기능을 붙일 수 있어 구역이 겹치지 않는다.
한 기능의 훅이 오류를 던져도 `runHooks` 가 삼켜 세계가 멈추지 않는다.

스킬은 `.claude/skills/` 에 있어 저장소와 함께 이동한다.

| 순서 | 스킬 | 담당 | 동시 실행 |
|---|---|---|---|
| — | `ksk-contract` | **공통 계약 (모두 먼저 읽는다)** | — |
| ~~0~~ | ~~`ksk-tabs`~~ | F7.2·F7.3 탭·대시보드 | **완료** (2026-09-08) |
| ~~1~~ | ~~`ksk-realestate`~~ | F2 부동산 매매 | **완료** (2026-09-08) |
| ~~1~~ | ~~`ksk-household`~~ | F4 살림살이 | **완료** (2026-09-08) |
| 1 | `ksk-jobs` | F8 직업·월급 | ✅ |
| ~~2~~ | ~~`ksk-mining-infra`~~ | F3 건물=채굴장 | **완료** (2026-09-08) |
| 2 | `ksk-monthly` | F5·F6.2 월 정산 | F2·F4·F8 완료 후 |
| 상시 | `ksk-verify` | 검증 절차 | 각 작업 끝에 |

**1단계 셋(F2·F4·F8)은 서로 의존이 없어 진짜로 동시에 돌릴 수 있다.**

**범위에 대한 솔직한 메모.** F0~F8 을 다 만들면 생활 시뮬레이션 한 편 규모다. 하루에 끝나지 않는다.
그래서 이 문서는 **전체 지도**로 두고, 실제 작업은 위 순서대로 **한 번에 한 기능씩** 완성한다.
각 기능은 끝난 시점에 화면에서 동작해야 하고, 동작하지 않는 반쪽 기능을 쌓아 두지 않는다.

**F7.2(탭)를 앞당길 수도 있다.** 부동산·살림 패널이 붙으면 한 화면에 다 담기지 않는다.
F2가 끝나는 시점에 판단한다.

## 7. 경제 수식

```
희소성   = 상한 ÷ (상한 − 채굴량)                   // 캘수록 커진다
수요     = 채택률(세계 연수) 기준 평균회귀 랜덤워크   // 외부 데이터 없음
원가하한 = 전기단가 × 60kWh ÷ 100만 KSK              // 이 아래로는 아무도 안 판다
시세     = max(기준가 × 희소성 × 수요, 원가하한) × (1 ± 슬리피지) × (1 ± 잡음)
비용/초   = (전기단가 × 총kW + 통신비 ÷ 730 × 폰대수) × 운영효율계수
운영효율계수 = 1 − (1 − 건물 cool) × (배치한 폰 ÷ 전체 폰)   // 미배치·민원 중이면 1
회선 한도 = max(1, round(건물 kw × 6))                       // 토지(kw 0)는 채굴장 불가
1단계 수익/초 = 풀지분 × 블록보상 ÷ 17.52 × 시세
2단계 수익/초 = 스테이크지분 × 블록보상 ÷ 17.52 × 시세
```

- 매도하면 시세가 눌리고 매수하면 밀려 오른다(슬리피지). 이후 몇 초에 걸쳐 되돌아온다.
- 채굴이 남으면 채굴자가 몰려 네트워크 해시레이트가 오르고(난이도 상승), 적자면 빠져나간다.
  그래서 내 지분은 가만히 두면 줄어들고, 장비·건물을 올려야 유지된다.

## 8. 기술 요구사항

- 단일 `index.html`. 외부 라이브러리·CDN·`<link>`·빌드 도구 없음.
  `lang="ko"`, `charset="UTF-8"`, viewport 메타 포함.
- **네트워크 요청 없음** — 완전 오프라인 동작(현실 경제 연동을 뗀 뒤로).
- 리셋 CSS + 어두운 배경 + `Segoe UI` 계열 — 기존 게임 3개와 같은 톤.
- 차트는 `canvas` 2D 직접 그리기.
- `localStorage` 저장. 저장이 막힌 환경(`data:` URL 등)에서도 `try/catch`로 화면이 깨지지 않는다.
- 모바일 반응형: 860px에서 2단→1단, 480px 이하에서 버튼 최소 높이 46px.
- 저장 키는 발행 설계가 바뀔 때 올린다 (`ksk_coin_v2`) — 옛 저장값이 새 규칙과 섞이지 않게.

## 9. 확장 방향 (v1 범위 밖)

- **v2 — 서버 한 겹**: 원장을 서버로 옮긴다. 잔액 이전은 "빼기와 더하기"가 한 묶음으로 성공해야 하므로
  **트랜잭션(ACID)이 실제로 필요한 첫 사례**가 된다. 엔드포인트·JSON 계약·CORS를 여기서 쓴다.
- **v3 — 다중 사용자**: 여러 채굴자가 같은 체인과 같은 부동산 시장을 두고 경쟁한다.
  전체 해시레이트가 난이도를 올리므로 남이 많이 캐면 내가 불리해진다. 로그인은 Supabase에 맡긴다.
- 그 외: 반감기 연출, 거래소 호가창, 채굴 풀, 도시 확장, 이웃 NPC.
- **끝까지 만들지 않을 것**: 실제 토큰 발행, 현금 입출금, 실명 자산 거래, 실제 시세 연동, 투자 조언.
