# PRD: KSK 코인 — 채굴과 가상경제 시뮬레이터

- 작성일: 2026-09-08
- 배포 형태: 단일 `index.html` (HTML/CSS/JS, 외부 라이브러리 없음) — 브라우저에서 즉시 실행, GitHub Pages로 온라인 배포

## 1. 개요

브라우저에서 **진짜 SHA-256 작업증명(PoW)** 을 돌려 가상화폐 `KSK` 를 채굴하고, 캔 코인으로 전기요금을 내고 장비를 사며 살아남는 1인 채굴자 시뮬레이터다.

핵심은 이 세계가 **현실 경제 데이터를 실제로 읽어 와 규칙에 반영한다**는 점이다. 두 개의 창으로 현실이 들어온다.

| 현실 데이터 | 들어오는 창 | 세계에 미치는 영향 |
|---|---|---|
| USD/KRW 환율 | **공급측** | 한국은 에너지를 수입한다 → 환율이 오르면 전기요금 단가가 오르고 채굴 원가가 올라간다 |
| 실제 BTC 24시간 변동률 | **수요측** | 실제 코인 시장이 오르면 이 세계의 매수 심리도 같이 오른다 |

### 이것은 시뮬레이션이다

실제 블록체인에 토큰을 발행하지 않고, 실제 화폐가 드나들지 않으며, 지갑·서명·P2P 네트워크도 없다. `KSK` 와 게임 내 원화는 **아무런 현실 가치가 없는 숫자**다. 실명 자산 거래·현금 입출금은 v1 이후에도 만들지 않는다.

## 2. 목표

1. **채굴을 가짜 진행바로 만들지 않는다.** 실제로 SHA-256 해시를 수백만 번 돌려 목표값 이하의 논스를 찾는다. 블록이 나오는 순간은 계산의 결과이지 타이머가 아니다.
2. **희소성을 실제 규칙으로 구현한다.** 총 발행 상한, 반감기, 난이도 자동 조정 — 비트코인의 구조를 1/1000 축척으로 옮긴다.
3. **경제 순환의 고리를 닫는다.** 채굴하면 전기요금이 나가고, 요금을 내려면 코인을 팔아야 하고, 팔면 시세가 눌린다. 벌고-쓰고-노출되는 고리가 있어야 경제다.
4. **현실 데이터가 실제로 판단을 바꾸게 만든다.** 환율이 오르면 큰 장비가 먼저 적자로 돌아서는 것을 화면에서 볼 수 있어야 한다.
5. 배운 것을 한 앱에 잇는 출발점을 만든다 — 오늘은 프론트, 다음은 서버·DB·로그인.

## 3. 대상 사용자 / 플랫폼

- 대상: 채굴과 코인 경제가 어떤 원리로 돌아가는지 손으로 만져 보고 싶은 비전공자. 사전 지식 없이 시작할 수 있어야 한다.
- 플랫폼: 데스크톱/모바일 브라우저(Chrome·Edge·Firefox·Safari 최신). `index.html` 더블클릭만으로 동작하고, 서버·설치·회원가입이 없다.
- 오프라인: 네트워크가 없거나 API가 실패하면 내장 스냅샷(`USD/KRW 1345.38`, `BTC 24h -1.47%`)으로 폴백하고 화면에 "오프라인 값" 을 표시한다.

## 4. 핵심 사용자 플로우

1. 파일을 연다 → 시작 화면에서 세계 설명과 "이건 시뮬레이션" 고지를 읽는다.
2. `채굴 시작` → 실제 해시 탐색이 돌고 해시레이트가 뜬다. 화면은 멈추지 않는다.
3. 블록을 찾으면 보상 `KSK` 가 들어오고 원장에 한 줄 쌓인다. 동시에 전기요금이 잔액에서 빠져나간다.
4. 현금이 마르면 시장에서 코인을 팔아 요금을 낸다 → 판 만큼 시세가 눌리는 것을 본다.
5. 여유가 생기면 장비를 사서 속도를 올린다 → 전력 소비도 같이 올라 손익분기가 이동한다.
6. 현실 환율/시세가 갱신되면 전기단가와 심리가 바뀌고, 방금까지 흑자였던 장비가 적자로 돌아설 수 있다.
7. 파산(현금 0 이하 + 팔 코인 없음)하거나 총 발행 상한에 도달하면 결과 화면으로 간다.

## 5. 화면별 요구사항

### 5.1 시작 화면
- 제목 `KSK COIN`, 한 문단 세계 설명.
- **시뮬레이션 고지**: 실제 화폐·실제 블록체인이 아니라는 문장을 눈에 띄게.
- 현실 데이터 로딩 상태 표시(`환율 불러오는 중…` → `USD/KRW 1,345.4 · BTC 24h -1.47%` 또는 `오프라인 값 사용`).
- `채굴 시작` 버튼. 시작 자금 500,000원 안내.

### 5.2 상단 지표 바 (HUD)
항상 보이는 6개: 보유 `KSK` / 보유 현금 / `KSK` 시세 / 해시레이트 / 난이도 / 채굴 진행률(발행량 대비).

### 5.3 채굴 패널
- 현재 목표(난이도)와 시도한 논스 수, 최근 해시 앞 16자리를 흘려 보여준다 — 진짜 계산 중임을 눈으로 확인.
- `채굴 시작 / 정지` 토글.
- 다음 반감기까지 남은 블록 수.
- 초당 전기요금과 초당 기대수익을 나란히 보여줘 **흑자·적자를 즉시 판단**할 수 있게 한다.

### 5.4 시장 패널
- `KSK` 시세와 직전 대비 변화. 최근 60개 가격을 `canvas` 스파크라인으로.
- 매도/매수 버튼(10% / 전량). 체결 시 슬리피지 적용.
- 가격이 왜 그 값인지 분해해서 보여준다: `기준가 × 희소성 × 심리` 와 `원가하한` 중 큰 값.

### 5.5 장비 패널
- 3종(노트북·GPU 리그·ASIC), 각 항목에 속도 배수 / 소비전력 / 가격 / 현재 대수.
- 구매 버튼. 현금 부족 시 비활성.
- 각 장비의 **현재 환율 기준 손익분기 시세**를 표시 — 환율이 오르면 이 숫자가 올라가는 것이 이 앱의 핵심 학습 지점이다.

### 5.6 원장(로그)
- 블록 발견·매매·구매·요금 납부를 시간순 한 줄씩. 최근 30줄만 유지.

### 5.7 결과 화면
- 파산 또는 발행 완료 시: 총 채굴량, 최종 순자산, 버틴 블록 수, 최고 시세, 다시 시작 버튼.

## 6. 경제·게임 로직 요약

### 6.1 작업증명 (진짜 계산)
- 순수 JS 로 구현한 동기식 SHA-256 을 쓴다. `crypto.subtle.digest` 는 비동기라 논스마다 `await` 하면 속도가 수십 배 느려지고, `file://` 에서 보안 컨텍스트 문제가 생길 수 있어 쓰지 않는다.
- 블록 헤더 = `{높이}|{이전해시}|{보상}|{논스}` 를 해시한다.
- 성공 판정은 비트코인과 같은 **목표값(target) 방식**: 해시 앞 8자리(32비트)를 정수로 보고 `target = 2^32 / 난이도` 이하면 블록 성공. 선행 0 개수 방식은 한 단계가 16배로 뛰어 미세 조정이 불가능하다.
- 메인 스레드를 막지 않기 위해 한 프레임에 정해진 개수만 시도하고 `requestAnimationFrame` 으로 양보한다. (Web Worker 는 단일 파일 + `file://` 조합에서 Blob URL 이 막힐 수 있어 v1 에서는 쓰지 않는다.)

### 6.2 발행과 난이도 — 비트코인 1/1000 축척
| 항목 | 비트코인 | KSK |
|---|---|---|
| 총 발행 상한 | 21,000,000 | **21,000** |
| 초기 블록 보상 | 50 | **50** |
| 반감기 간격 | 210,000 블록 | **210 블록** |
| 목표 블록 시간 | 10분 | **20초** |
| 난이도 조정 주기 | 2,016 블록 | **10 블록** |

- 난이도 조정: 최근 10블록의 실제 소요시간을 목표시간과 비교해 비율만큼 조정하고, 한 번에 4배/¼배를 넘지 않게 제한한다(비트코인과 동일한 안전장치).
- 장비 배수는 **난이도 목표를 그만큼 완화**하는 방식으로 구현한다. `N배 빠른 기계가 난이도 D를 푸는 기대시간`과 `1배 기계가 난이도 D/N을 푸는 기대시간`은 근사가 아니라 **정확히 같다**(둘 다 `D / (해시속도 × N)`). 그래서 해시는 언제나 진짜로 돌면서 장비 효과가 정확히 반영된다. 표시 해시레이트 = 실측 × 배수.

### 6.3 현실 데이터 주입
```
전기단가   = 기준단가 × (USD/KRW ÷ 1345.38)      // 공급측
심리       = 1 + (BTC 24h 변동률 ÷ 100) × 2       // 수요측, 0.5~1.8 로 제한
```
- 시작 시 1회 조회, 이후 3분마다 갱신. 실패 시 마지막 성공값 유지, 처음부터 실패면 스냅샷.
- 출처: `open.er-api.com`(환율), `api.coingecko.com`(시세). 둘 다 API 키 불필요, `access-control-allow-origin: *` 확인됨.

### 6.4 가격 결정
```
희소성   = 상한 ÷ (상한 − 채굴량)                 // 캘수록 커진다
원가하한 = 전기단가 × 블록당_소비전력 ÷ 블록보상   // 이 아래로는 아무도 안 판다
시세     = max(기준가 × 희소성 × 심리, 원가하한) × (1 ± 노이즈)
```
- 매도하면 시세가 소폭 하락, 매수하면 소폭 상승(슬리피지). 이후 몇 초에 걸쳐 되돌아온다.

### 6.5 전기요금
- 채굴 중일 때만 초당 `전기단가 × 총소비전력` 이 현금에서 빠진다.
- 현금이 0 이하가 되면 채굴이 자동 정지되고 경고가 뜬다. 코인을 팔면 재개할 수 있다.
- 현금 0 이하이면서 보유 코인도 0 이면 파산.

## 7. 기술 요구사항

- 단일 `index.html`. 외부 라이브러리·CDN·`<link>`·빌드 도구 없음. `lang="ko"`, `charset="UTF-8"`, viewport 메타 포함.
- 리셋 CSS + 어두운 배경 + `Segoe UI` 계열 — 기존 3개 게임과 같은 톤.
- 차트는 `canvas` 2D 직접 그리기.
- 진행 상황은 `localStorage` 에 저장하고 다음 방문에 이어서 한다. `초기화` 버튼 제공.
- 모바일 반응형: 480px 이하에서 패널이 한 줄로 쌓인다.
- 네트워크 실패·JSON 형식 변경에 대해 화면이 깨지지 않아야 한다(항상 폴백).

## 8. 현재 구현 상태 (v1, `index.html` 기준)

- [ ] 순수 JS SHA-256 + 목표값 방식 PoW, 프레임 단위 양보
- [ ] 발행 상한 / 반감기 / 난이도 자동 조정
- [ ] 실제 환율·시세 조회와 오프라인 폴백
- [ ] 가격 엔진(희소성 × 심리, 원가하한, 슬리피지)
- [ ] 전기요금·파산 판정
- [ ] 장비 3종과 손익분기 시세 표시
- [ ] 원장 로그, 스파크라인, `localStorage` 저장

## 9. 향후 개선 제안 (Out of scope for v1)

- **v2 — 서버 한 겹**: 원장을 서버로 옮긴다. 잔액 이전은 "빼기와 더하기" 가 한 묶음으로 성공해야 하므로 **트랜잭션(ACID)이 실제로 필요한 첫 사례**가 된다. 엔드포인트·JSON 계약·CORS 를 여기서 쓴다.
- **v3 — 다중 사용자**: 여러 채굴자가 같은 체인을 두고 경쟁한다. 전체 해시레이트가 난이도를 올리므로 남이 많이 캐면 내가 불리해진다. 로그인은 Supabase 에 맡긴다(직접 만들지 않는다).
- **v4 — 자동 카드/스킬 연동**: 강의노트 요약에서 개념 카드를 뽑는 스킬과 이어 붙인다.
- 그 외: 반감기 이벤트 연출, 거래소 호가창, 채굴 풀, 세금, 실제 뉴스 헤드라인을 이벤트로 주입.
- **끝까지 만들지 않을 것**: 실제 토큰 발행, 현금 입출금, 실명 자산 거래, 투자 조언.
